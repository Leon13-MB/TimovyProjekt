<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TP_eKasa.Views.Advertxt">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <ListView x:Name ="advertxtView" ItemSelected="ItemSelect" ItemsSource="{Binding advertxt}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding ADVERTEXT}"/>
                                <Label Text="{Binding ADVERTISEMENT_TEXT}"/>
                                <Label Text="{Binding ADVERTISEMENT_TYPE}"/>
                                <Label Text="{Binding ADVERTISEMENT_LOOKUP}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>